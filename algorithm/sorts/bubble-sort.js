const numbers = [99, 44, 6, 2, 1, 5, 63, 87, 283, 4, 0];

function bubbleSort(array) {
    //Code here
    // return array.sort((a, b) => {
    //     if (a > b) {
    //         return a - b;
    //     } else if (a < b) {
    //         return b - a;
    //     } else {
    //         return 0;
    //     }
    // });
    let index = 0;
    for (let n of array) {
        
    }
}

bubbleSort(numbers);
console.log(numbers);